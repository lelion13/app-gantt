# Backend (app-gantt)

Paquete Python para la API FastAPI (se completa desde la **fase 1** en adelante).

## Herramientas (fase 0)

```bash
python -m venv .venv
pip install -e ".[dev]"
ruff check app && ruff format app
black --check app
```
